package com.avaliadorimovel

import com.avaliadorimovel.details.DetailsActivity
import com.avaliadorimovel.details.repository.SampleItem
import org.junit.Test

/**
 * Example local unit test, which will execute on the development machine (host).
 *
 * See [testing documentation](http://d.android.com/tools/testing).
 */
class UnitTestStatisticalCalculator {
    @Test
    fun `when calculateFactors is called, return populated listSample`() {
        val sampleList: ArrayList<SampleItem> = DetailsActivity.
    }
}